# Base API URL for Backend REST endpoints
VITE_API_BASE_URL=http://localhost:8000

# Base WebSocket URL for real-time live-debug and verdict streaming
VITE_WS_BASE_URL=ws://localhost:8000

# Optional default LLM configuration hints (can also be entered directly in the dashboard UI)
# VITE_DEFAULT_LLM_BASE_URL=http://localhost:4001
# VITE_DEFAULT_LLM_API_KEY=sk-1234
# VITE_DEFAULT_LLM_MODEL=gpt-4o-mini
